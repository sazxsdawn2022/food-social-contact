server:
  port: 8080

spring:
  application:
    name: ms-registry

# 配置 Eureka Server 注册中心
eureka:
  client:
    register-with-eureka: false  #自我注册，自我发现取消
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8080/eureka/